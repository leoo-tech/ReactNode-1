// Definição da idade (substitua pelo valor desejado)
let idade = -5; // Exemplo de idade inválida

// Verificação da validade da idade
if (idade < 0 || idade > 130) {
    console.log("Idade inválida! A idade deve ser um número entre 0 e 130.");
} else {
    console.log("Idade válida.");
}
